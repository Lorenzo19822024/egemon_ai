# Messaggio da TM5 per TM3 — 16 Marzo 2026

## Sessione 15 — EGEMON Forge: architettura completa

### Cosa abbiamo fatto

**egemon-forge** — commit `581ac9a` pushato su `Lorenzo19822024/egemon-forge`

1. **Nuovo modulo DISCOVER** — primo passo della Progettazione. Conversazione guidata con AI su 8 aree (problema, attori, conoscenza, regole, dati, risultato, qualità, eccezioni). Meccanismo PIN: l'utente accetta elementi proposti dall'AI → salvati nel Knowledge Repository (BLURB, Building Block, Factor). Ponte tra "ho un'idea" e "so progettare".

2. **Nuovo modulo CT STRATEGY** — configurazione della strategia Frictionless del Cognitive Twin. 7 sezioni: Intent Decoder, Profiling & Adattamento, Routing Strategy (DEEP/FAST/AUTO), Dual Mode, Onboarding, Citations & Traceability, Feedback Loop. Demo precaricata Credit Q-Screen.

3. **BLURB estesi con Formation** — 4° componente (oltre a Rules, Scores, Data). Il componente Formation è il Dual Mode: come spiegare il BLURB all'utente. Formazione ed esecuzione dalla stessa KB.

4. **Blueprint FlowNode estesi** — ogni nodo ora ha `routingStrategy` (deep/fast/auto) e `contextBlurbs` (quali BLURB attivare a quel passo per il Context Scoping).

5. **Sidebar riorganizzata** — due gruppi: PROGETTAZIONE (Discover → Blueprint → KT → BLURB → Thesaurus → Data Req → Artefact → CT Strategy → Prompt Workshop → Test Bench → Deploy) e SERVIZIO (Excel Importer, Genome, Guardrails, Dashboard, Docs).

6. **Start Here ridisegnata** — hero, 3 momenti visual (Progettazione → Realizzazione → Esecuzione → Autopoiesi), card numerate per Progettazione, card Servizio.

7. **NEXT/BACK navigation** — barra navy fissa in basso su ogni modulo con navigazione sequenziale.

8. **IDB versione 5** — nuovo object store `ctstrategies`.

### Metodologia HTML

Creato `STRATEGIA/EGEMON-Forge-Metodologia.html` — documento continuo (no Paged.js) che spiega l'intera metodologia Forge. 10 sezioni: cover, introduzione (per non-tecnici), Discover, il problema, i 3 momenti, destrutturazione, configurazione, CT Strategy, realizzazione, esecuzione + autopoiesi. Non in repo — locale in STRATEGIA/.

### Problema Gödel — serve fix bridge

Il bridge Gödel su VPS (`https://d-nd.com/godel-tm5-api/ask`) elabora le richieste correttamente (vedo nella history: 34-46 secondi, exit_code 0, has_meta true) **ma non restituisce la risposta al chiamante**. Il primo response è `{"status":"processing"}` poi la connessione si chiude. Non c'è un endpoint per recuperare la risposta a posteriori.

**Richiesta**: una di queste soluzioni:
- Aumentare il timeout del proxy nginx per `/godel-tm5-api/` (serve almeno 60 secondi)
- Aggiungere un endpoint GET `/godel-tm5-api/read/:id` che restituisce answer + meta di una richiesta già elaborata
- Entrambe

### Architettura — il "giro del fumo" completo

Abbiamo definito e documentato il flusso end-to-end:

```
IDEA → DISCOVER (AI+PIN) → KB → FORGE (destruttura+configura+CT Strategy)
  → PROMPT WORKSHOP → DATA INTEGRATION → TEST → DEPLOY
    → APP LIVE (CT come strategia Frictionless)
      → FEEDBACK → AUTOPOIESI → KB aggiornata → ↺
```

Concetto chiave emerso: **il CT non è il prodotto — è la strategia Frictionless** con cui il prodotto elimina l'attrito tra l'utente e il risultato. Il prodotto è l'app deployata.

---

*Da TM5 (Lorenzo + Claude Code) — 16 Marzo 2026*
